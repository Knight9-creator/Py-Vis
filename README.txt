#README Paragraph:

#MC-01
#Originally, Script failed with KeyError, global_sales.
#After the renaming fix, KPIs and charts run correctly.

#MC-02
#We show missing values before/after printout.
#We made it so that charts compute without NaN errors.

#MC-03
#We showed row counts before/after dedupe.
#Market share values adjust appropriately.

#MC-04
#We used .div() with fillna(0) to handle cases where a genre count might be 0.

#MC-05
#We use .str() and replace methods to clean the code with text instead of integers. 

#MC-10
#We commented the line that contained the powerup sound effect to play.

#MC-12
#We detect if the user clicks once, and if so, we make the player jump, and wait for another jump input.